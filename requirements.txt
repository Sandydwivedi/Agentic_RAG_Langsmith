# Core
streamlit>=1.32.0
python-dotenv>=1.0.0

# LLM + Agent framework
groq>=0.9.0
langchain==0.2.16
langchain-community==0.2.16
langchain-core==0.2.38
langchain-text-splitters==0.2.2
langchain-groq>=0.1.6
langchain-huggingface>=0.0.3
langgraph>=0.1.0

# Vector store
faiss-cpu>=1.8.0

# Embeddings (runs locally, no API key needed)
sentence-transformers>=2.7.0

# PDF loading
pymupdf>=1.24.0

# OCR for scanned PDFs (optional but recommended)
# Note: also requires the tesseract-ocr SYSTEM binary installed separately
# (apt install tesseract-ocr / brew install tesseract) — pip alone isn't enough.
pytesseract>=0.3.10
Pillow>=10.0.0

# Web search
duckduckgo-search>=6.1.0   # fallback if TAVILY_API_KEY isn't set
tavily-python>=0.3.0       # preferred — needs TAVILY_API_KEY in .env
